class Provider {
    /**
     * Anything put here should not depend on any other classes
     */
    register() {
    }

    /**
     * Called when the application boots
     */
    boot() {
    }
}

module.exports = { Provider };
